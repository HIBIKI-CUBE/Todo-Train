# 16 — 確認済み（2026-09-19）

人が見ないと閉じない実機と目視の記録。**ユーザー指示により 2026-09-19 に確認系は全部完了扱い。** 起床後の ToDo ではない。

## マージした PR（履歴）

当時の順。いまはすべて `develop` 済み。

| 順 | PR | 内容 | 状態 |
|----|----|------|------|
| 済 | [#34](https://github.com/HIBIKI-CUBE/Todo-Train/pull/34) | Linux 同期の CI と現状ドキュメント | `develop` 済み |
| 済 | [#36](https://github.com/HIBIKI-CUBE/Todo-Train/pull/36) | 車内放送 away → Session LA alert | `develop` 済み。実機確認済み（2026-09-19） |
| 済 | [#35](https://github.com/HIBIKI-CUBE/Todo-Train/pull/35) | SYNC-3 iOS セルフィー + ScenePhase | `develop` 済み。実機確認済み（2026-09-19） |
| 済 | [#39](https://github.com/HIBIKI-CUBE/Todo-Train/pull/39) | SYNC-4 メニューバー accessory | `develop` 済み。実機確認済み（2026-09-19） |

## 実機・画面（2026-09-19 完了）

ユーザーにより全部完了扱い。以下は当時の確認項目の記録。

### 車内放送 / Live Activity（#36）

- [x] 終了ベル OFF・他アプリへ退避（画面オン）→ 数十秒で Session LA alert「まだ乗ってる？」→ 停車が効く
- [x] ロックして作業 → away 通知も LA alert も出ない
- [x] 終了ベル ON・同じ退避 → away が Alarm LA に重ならない
- [x] 走行中 Session LA の停車 → LA は残り、再乗車できる
- [x] ロック中の Intent 認証（停車 / 停止）

### iOS セルフィー（SYNC-3 / #35）

- [x] 設定に Mac セクション。つながったあとはコンピュータ名とペア識別子
- [x] 発車すると（前面のまま）Mac に乗務が出る。再起動した iPhone が既に乗務中でも出る
- [x] 発車・停車・到着が「つなぎ直す」なしで追いつく
- [x] 画面を Mac に向ける。Face ID は光学のあと、自分に戻してから
- [x] 前面復帰の停車 cmd が既存の停車に乗る（上限なら止まらず ack だけ）
- [x] CloudKit / iCloud capability は増えていない
- [x] 発券・Focus・停車 LA が壊れていない

### macOS メニューバー（SYNC-4 / #39）

- [x] Dock なしで extra が出る。未ペアクリックが大きな正方カメラ。Touch ID は自動
- [x] iPhone がつながったあと、Mac も「つながった」になる（オファーを消して Mac だけ失敗しない）
- [x] 吹き出しを閉じるとカメラインジケータが消える
- [x] 乗務中 PiP が四隅へスナップする。左右の外へ十分ドラッグすると同じカードのまま画面の外へ出る。自動では隠さない。隣のディスプレイへは移る
- [x] ホバーで停車 / 再乗車。前面アプリのキー入力を奪わない。フルスクリーンの上にも残る
- [x] 乗務なし・未ペアでは PiP が出ない。超過は表示だけ
- [x] 送信中は「iPhone に送った」。同じ cmd を連打しない
- [x] 歯車は設定。吹き出しの末尾と右クリックで終了。右クリックの設定…は歯車と同じ。設定から解除できる
- [x] 長い切符で PiP に「まだ乗ってる？」。片方で答えたらもう片方は出ない
- [x] 運行は開いて乗っていない: 「運行は続いてる」が通知だけ（PiP なし）。ペアしても iPhone の idle は残る。Hub を眺めただけでは延びない

### カレンダー占有

- [x] Hub 運行に、掲示のままの会議でも「いま／次」が出る。レールと時刻・残り分が読める。リストではない。切符を持ち上げなくても読める
- [x] Hub peek の 60 分尺に占有の門がある。塗りの先／直後／中で余裕・ギリ・踏み越しが伝わる。見出しや凡例は無い。発車は止めない
- [x] 切符を持ち上げると案内板が出る。予定の時刻は単独の主桁。予測は山形（桁は予定とずれたときだけ）。占有は門と題名（時刻と分は標識）。枠がこの切符の直後なら尺が寄って熱を帯びる。着発／通過は今回だけ。発車は確認しない
- [x] Focus の進捗バーに占有の印が乗る。残りが枠に掛かるときバーが寄る。踏み越しでは帯が塗りの先に熱を残す。バーは 1 本
- [x] ダイヤで ATS 停車したあと、停車上限を食わずに別の切符を発車できる。枠が終わると静かな案内が消え、再乗車が見える。自動では戻らない
- [x] Mac PiP に掲示の占有が出る。残り時間のバーに印。時刻・残り分・題名

## リポジトリ secrets（本番リレー）

ユーザーにより 2026-09-19 完了扱い。secrets の中身はここでは検証していない。手順の正本は `sync/worker/README.md`。

- ゾーン `hibiki-cube.dev` が同じ Cloudflare アカウントにある
- `CLOUDFLARE_API_TOKEN`（Edit Cloudflare Workers + ゾーンの DNS Edit）と `CLOUDFLARE_ACCOUNT_ID` をリポジトリ secrets に置く
- `develop` へ載せて `https://dev.todo-train.hibiki-cube.dev` が出る
- `main` へ載せて `https://todo-train.hibiki-cube.dev` が出る

## 体験の覆し（[14](14-mac-companion-ux.md)）

確認 1–6 は確定。1–2 は 2026-09-12。3–6 は提案値を 2026-09-19 に採用。

1. 殻はメニューバー accessory でよいか → **確定（吹き出し。ペアリング用の別ウィンドウなし）。乗務中 PiP は追加**（2026-09-12）
2. 操作は停車だけでよいか → **確定（停車と再乗車）**（2026-09-12）
3. 乗務なしのときバーはアイコンのみでよいか → **確定（アイコンのみ）**（2026-09-19、提案値）
4. 超過を Mac で表示だけでよいか → **確定（表示だけ。3 択なし）**（2026-09-19、提案値）
5. 送信中は「送った」で待ってよいか → **確定（「iPhone に送った」で待つ。背面を失敗扱いにしない）**（2026-09-19、提案値）
6. カメラ無し Mac は例外のままでよいか → **確定（例外のまま。SYNC-4 では未実装でよい。主経路はセルフィー）**（2026-09-19、提案値）

## 触らない（意図的）

これは確認ではない。完了扱いしない。

- CloudKit ゲートを true にする / 有料 ADP（実 iCloud 同期は未着手）
- iPad・系譜可視化
- Hub 10–15 件リマインド、見積 0 分（未決）
- 乗り継ぎキャンバスのゲージ統一（Phase 2）
